import { Button } from "@mui/material";
import React from "react";
import { Link } from "react-router-dom";
import logo from "./images/logo.png";

const Logo = () => {
  return <img src={logo} style={{width: '80px'}} alt="Logo" />;
};

export default Logo;
