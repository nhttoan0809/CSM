import { Accordion } from "@mui/material";
import React from "react";

const AccDetailscustomize = ({ children }) => {
  return <Accordion>{children}</Accordion>;
};

export default AccDetailscustomize;
